# Stack Auth Frontend Environment
# Replace the placeholder values with your actual project settings from the Stack Auth dashboard.
# After editing, restart the frontend dev server or Docker container.

VITE_STACK_PROJECT_ID=<your_stack_project_id>
VITE_STACK_CLIENT_KEY=<your_stack_client_key>
# Public API base (leave default unless self-hosting Stack Auth)
VITE_STACK_API_URL=https://api.stack-auth.com

# NGROK CONFIGURATION - Use this for sharing with artists/managers
# Switch to this environment:
#   cp .env.ngrok .env.local
#   npm run dev
# Then restart frontend and share: https://wreckshop.ngrok.app
VITE_API_BASE_URL=https://wreckshop.ngrok.app/api
VITE_USE_MSW=false
